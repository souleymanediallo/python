# Generated by Selenium IDE
import pytest
import time
import json
from selenium import webdriver
from selenium.webdriver.common.by import By
from selenium.webdriver.common.action_chains import ActionChains
from selenium.webdriver.support import expected_conditions
from selenium.webdriver.support.wait import WebDriverWait
from selenium.webdriver.common.keys import Keys
from selenium.webdriver.common.desired_capabilities import DesiredCapabilities


class TestLinks():
    def setup_method(self, method):
        self.driver = webdriver.Chrome()
        self.vars = {}

    def teardown_method(self, method):
        self.driver.quit()

    def wait_for_window(self, timeout=2):
        time.sleep(round(timeout / 1000))
        wh_now = self.driver.window_handles
        wh_then = self.vars["window_handles"]
        if len(wh_now) > len(wh_then):
            return set(wh_now).difference(set(wh_then)).pop()

    def test_lINKSPAGE(self):
        self.driver.get("https://clonechatgptsd.pythonanywhere.com/projet")
        self.driver.set_window_size(1895, 991)
        self.driver.find_element(By.LINK_TEXT, "Projet").click()
        self.vars["window_handles"] = self.driver.window_handles
        self.driver.find_element(By.LINK_TEXT, "Github").click()
        self.vars["win9075"] = self.wait_for_window(2000)
        self.vars["root"] = self.driver.current_window_handle
        self.driver.switch_to.window(self.vars["win9075"])
        self.driver.switch_to.window(self.vars["root"])
        self.driver.find_element(By.CSS_SELECTOR, ".h-8").click()

    def test_promptquestion(self):
        self.driver.get("https://clonechatgptsd.pythonanywhere.com/")
        self.driver.set_window_size(1512, 880)
        self.driver.find_element(By.ID, "prompt").click()
        self.driver.find_element(By.ID, "prompt").send_keys("écrire la fonction print en python")
        self.driver.find_element(By.ID, "send-icon").click()

